# Configuración de la API del Backend
# Copia este contenido a un archivo llamado .env en la raíz del proyecto

# Para desarrollo local (cuando el backend corre en localhost):
VITE_API_URL=http://localhost:3001/api

# Para producción (cuando el backend está en Vercel):
# Reemplaza con tu URL real de Vercel, ejemplo:
# VITE_API_URL=https://administrarcolores-back.vercel.app/api

